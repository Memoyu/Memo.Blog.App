@inherits LayoutComponentBase

<MApp>
    <MMain>
        <MErrorHandler>
            <AuthorizeView>
                <Authorized>
                    <PPageStack TabbedPatterns="@( ["^/$", "/article", "/moment", "/user"])"
                                SelfPatterns="@( ["/article"])">
                        @Body
                    </PPageStack>
                </Authorized>
                <NotAuthorized>
                    @Body
                </NotAuthorized>
            </AuthorizeView>
        </MErrorHandler>
    </MMain>

    <BottomNavigation />
</MApp>

