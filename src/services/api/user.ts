import { LoginParams, UserLogin } from "@/types/interfaces/user";

export function login(data: LoginParams): Promise<any> {
  throw  new Error('待接口对接');
}

export function getUserInfo(): Promise<any> {
 throw  new Error('待接口对接');
}